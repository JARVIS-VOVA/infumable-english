import React from 'react'
import { Box, Typography } from '@mui/material'

const Word = () => (
  <Box>
    <Typography variant='body2' sx={{ mt: 1 }}>
      [teacher - вчитель]
    </Typography>
    <Typography variant='body2' sx={{ mt: 1 }}>
      [student - студент]
    </Typography>
  </Box>
)

export default Word
