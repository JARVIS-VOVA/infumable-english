import styled from 'styled-components'

import { CURIOUS_BLUE } from 'Constants/colors'

const Link = styled.a`
  color: ${CURIOUS_BLUE};
  text-decoration: none;
  font-style: normal;
  font-weight: normal;
`

export default Link
