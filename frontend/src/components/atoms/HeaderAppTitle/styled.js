import styled from 'styled-components'

import { CURIOUS_BLUE } from 'Constants/colors'
import { HORIZONTAL_PADDING } from 'Constants/size'

export const HeaderAppTitle = styled.h1`
  font-style: italic;
  font-weight: 600;
  color: ${CURIOUS_BLUE};
  padding-left: ${HORIZONTAL_PADDING}px;
`

export default HeaderAppTitle
